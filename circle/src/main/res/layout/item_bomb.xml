<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:baselineAligned="false"
    android:orientation="horizontal"
    android:background="?attr/itemBackground">

    <ImageView
        android:id="@+id/avatar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/topic_item_padding"
        android:src="@drawable/ic_avatar_1"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foreground="?android:attr/selectableItemBackground"
        android:orientation="vertical"
        android:paddingTop="@dimen/topic_item_padding"
        android:paddingBottom="@dimen/topic_item_padding"
        android:paddingRight="@dimen/topic_item_padding"
        android:paddingLeft="0dp">

        <TextView
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:text="Large Text"
            android:textSize="@dimen/topic_comment_text_size"
            android:textColor="@color/primary_text" />

        <TextView
            android:id="@+id/date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:text="Small Text"
            android:textSize="@dimen/topic_info_text_size"
            android:textColor="@color/secondary_text"/>

    </LinearLayout>

</LinearLayout>